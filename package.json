{
  "name": "restmon",
  "version": "0.1.0",
  "private": true,
  "description": "RestMon - REST API Monitoring and Orchestration Platform",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "echo 'Select a workspace to run dev'",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces",
    "test": "npm run test --workspaces"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "devDependencies": {
    "@types/cheerio": "^0.22.35",
    "@types/jmespath": "^0.15.2",
    "@types/js-yaml": "^4.0.9",
    "@types/xmldom": "^0.1.34"
  },
  "dependencies": {
    "cheerio": "^1.1.2",
    "jmespath": "^0.16.0",
    "xmldom": "^0.6.0",
    "xpath": "^0.0.34",
    "yaml": "^2.8.2"
  }
}
